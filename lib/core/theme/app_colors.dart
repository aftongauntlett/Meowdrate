import 'package:flutter/material.dart';

/// Ocean-toned palette carried over from the original prototype.
class AppColors {
  const AppColors._();

  static const background = Color(0xFF0B1220);
  static const surface = Color(0xFF101A2E);
  static const text = Color(0xFFF5F7FF);
  static const textMuted = Color(0xFFB8C0D9);
  static const primary = Color(0xFF6EE7FF);
  static const primaryPressed = Color(0xFF35D6F8);
  static const border = Color(0x1FF5F7FF);

  static const success = Color(0xFF6EE7FF);
  static const gold = Color(0xFFFFD166);
}
