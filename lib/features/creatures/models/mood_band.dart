/// Derived from the flood level (see moodBandProvider) — the same cat
/// picked for the day reacts to how full the flood still is, rather than
/// swapping to a different character.
enum MoodBand { sad, neutral, happy }
